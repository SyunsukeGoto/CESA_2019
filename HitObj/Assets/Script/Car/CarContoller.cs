using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace Kazu
{
    public class CarContoller : MonoBehaviour
    {
        const int DoNotMove = 180;
        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }

        public int GetTime()
        {
            return DoNotMove;
        }
    }
}

